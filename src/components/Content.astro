---
export interface Props {
  className?: string
}

const { className = '' } = Astro.props
---

<article
  class={`prose prose-lg dark:prose-invert prose-a:no-underline mx-auto max-w-5xl px-8 ${className}`}
>
  <slot />
</article>
