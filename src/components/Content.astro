---
export interface Props {
  className?: string
}

const { className = '' } = Astro.props
---

<article
  class={`prose prose-lg dark:prose-invert prose-a:no-underline mx-auto max-w-4xl px-8 ${className}`}
>
  <slot />
</article>
