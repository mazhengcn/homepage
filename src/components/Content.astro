---
import Prose from './Prose.astro'

export interface Props {
  className?: string
}

const { className = '' } = Astro.props
---

<Prose className={`mx-auto max-w-4xl px-8 ${className}`}><slot /></Prose>
